<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.laundryargan">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application

        android:allowBackup="true"
        android:icon="@mipmap/logo_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        android:name=".app.AppController"
        android:networkSecurityConfig="@xml/network_security_config">
        <uses-library android:name="org.apache.http.legacy" android:required="false"/>

        <activity android:name=".tampilan.UserActivity" />
        <activity android:name=".tampilan.DetailActivity" />
        <activity android:name=".tampilan.HeaderActivity" />
        <activity android:name=".tampilan.pelangganview" />
        <activity android:name=".tampilan.Login">

        </activity>

        <activity android:name=".activities.PelangganActivity" />
        <activity
            android:name=".tampilan.menu"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        </activity>
        <activity android:name=".tampilan.Laporan" />
        <activity android:name=".tampilan.pelangganActivity" />
        <activity android:name=".tampilan.BarangActivity" />
        <activity android:name=".tampilan.Argan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".tampilan.DataCuciMasuk" />
        <activity android:name=".MainActivity" />
        <activity android:name=".tampilan.RegisterActivity"/>
    </application>

</manifest>